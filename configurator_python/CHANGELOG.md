# Changelog

## 0.1.0 - Unreleased

- Add the typed Python runtime for generated Configurator scopes.
- Support weighted lookups, scope mutations, access events, and Dart-compatible
  defaults.
- Add optional Pydantic snapshot support for FastAPI and other service
  boundaries.
