# Changelog

## 0.1.0 - Unreleased

- Add the ESM TypeScript runtime for generated Configurator scopes.
- Support weighted lookups, scope mutations, access events, and Dart-compatible
  defaults in Node.js and browsers.
- Document the required build step for local-path consumers.
