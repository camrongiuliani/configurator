name: configurator_flutter
description: Flutter adapters, theming, and widgets for the Configurator weighted configuration runtime.
version: 1.0.20
publish_to: none
homepage: https://github.com/david-k-williams/configurator
repository: https://github.com/david-k-williams/configurator/tree/develop/configurator_flutter
issue_tracker: https://github.com/david-k-williams/configurator/issues

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  i18n_extension: ^15.1.1
  google_fonts: ^6.3.2

  parse_color: ^0.0.1
  collection: ^1.17.1
  yaml: ^3.1.2

  slang_flutter: ^3.0.0

  configurator: ^1.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
